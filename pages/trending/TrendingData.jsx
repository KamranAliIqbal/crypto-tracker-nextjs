

const TrendingData = ({ name, score, symbol, slug, image }) => {
  
  console.log(name)
  
  return (
    <div>
      <p className="">{symbol}</p>
      <p className="">{name}</p>
      </div>
    
  )
}

export default TrendingData
